include _bemto.pug
//mixin mixin_name(title)

mixin head(title)
    include _head.pug
    
mixin nav-item(link_text, active)    
    li(class= active ? "nav-list__item nav-list__item_active" : "nav-list__item")
        a(href="#").nav-list__link= link_text