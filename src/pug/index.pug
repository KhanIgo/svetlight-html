doctype html
include parts/_mixins.pug
html(lang='RU-ru')
    +head('Главная')
    
    body
        
        include parts/_header.pug
        
        
        
        
        include parts/_footer.pug
        
        
        script.
            jQuery(function($) {});
